import { PromptDirective } from './prompt.directive';

describe('PromptDirective', () => {
  it('should create an instance', () => {
    const directive = new PromptDirective();
    expect(directive).toBeTruthy();
  });
});
